{"probe":"grep-satisfies-read-before-edit","cc_version":"2.1.161","exit_code_convention":"exit_0_means_pass","expected_single_file_grep":"ALLOW","conclusion":"pass","method":"in-session-live","note":"profile b 2026-06-03, CC 2.1.161 (>=2.1.160): live-session single-file `grep beta <abs-path>` then Edit (no Read) returned 'updated successfully' — the >=2.1.160 ALLOW flip CONFIRMED on-binary; promotes the 2.1.159 BLOCK baseline (grep-read-state/2.1.159/outcome.json) -> 2.1.161 ALLOW. Subprocess probe path unavailable (machine is OAuth-only, no ANTHROPIC_API_KEY; seeding OAuth creds is the UNSAFE-rejected path per decisions.md 2026-05-24 row); captured via the same in-session method as the 2.1.159 baseline (M2/Probe-1e). NUANCE: the satisfier matches on the LITERAL file-arg string in the grep command — a first attempt with an UNEXPANDED env var ($CLAUDE_JOB_DIR/...) in the grep path BLOCKED (CC could not path-match the literal '$CLAUDE_JOB_DIR' string to the Edit's absolute target); re-run with a literal absolute path ALLOWed. Use resolved absolute paths.","observed_single_file_grep":"ALLOW","observed_multi_file_grep":"BLOCK","observed_grep_tool":"BLOCK","grep_tool_note":"Grep TOOL does NOT satisfy read-before-edit on 2.1.161 = BLOCK (closed 2026-06-03). A Grep-tool view then Edit with no Read hard-errored 'File has not been read yet'; control no-view Edit also BLOCKed (harness trustworthy); raw stream = Grep then Edit, no Read. Only bash grep/egrep/fgrep COMMANDS satisfy, never CC's Grep tool (matches the changelog's literal 'commands' wording). Method=live-dir claude -p child, no API key: the native Grep tool is absent from this interactive deferred-tools session AND a general-purpose subagent, but a standard claude -p against the LIVE CLAUDE_CONFIG_DIR exposes the full toolset (smoke GREP=yes) and auths via live OAuth — sidesteps the 2026-05-24 sandbox cred-seeding hazard (rotation writes back to the live dir, like a normal concurrent session).","ts":1780465370}
