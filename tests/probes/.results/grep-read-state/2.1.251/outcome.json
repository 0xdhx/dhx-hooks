{"probe":"grep-satisfies-read-before-edit","cc_version":"2.1.251","exit_code_convention":"exit_0_means_pass","expected_single_file_grep":"ALLOW","conclusion":"pass","method":"in-session-live","note":"2026-08-29, profile b, CC 2.1.251: the >=2.1.160 ALLOW flip STILL HOLDS. Script itself NOT run (machine remains OAuth-only, ANTHROPIC_API_KEY unset -> the script's clean `skipped` branch; seeding OAuth creds stays the UNSAFE-rejected path per decisions.md 2026-05-24). Captured via the same in-session method as the 2.1.159/2.1.161 baselines, run inside a dispatched subagent context with a BLOCKING no-view control (control BLOCK reproduced in three contexts: subagent, nested subagent, live-dir `claude -p` child) so the ALLOWs are trustworthy. Authoritative upstream CHANGELOG (fetched 2026-08-29, top version 2.1.251) shows NO revert or narrowing of the satisfier anywhere in 2.1.160..2.1.251; the only adjacent entry is 2.1.212's fix for a SPURIOUS 'File has not been read yet' on offset/limit reads before resume, which loosens the Read-tool satisfier and does not touch grep. CORRECTION to the 2.1.161 row's `literal file-arg string` nuance: that mechanism is WRONG. An unexpanded var is not itself disqualifying -- `grep PAT \"$HOME/f.txt\"` as a lone simple command ALLOWs (reproduced on two fresh files), while `$TMPDIR` and shell-locals BLOCK. The real discriminator is COMPOUNDING: any `;`-compound disqualifies even a literal absolute path with grep in first position (`D=/abs; grep PAT /abs/f.txt` -> BLOCK; `grep PAT /abs/f.txt; echo done` -> BLOCK). Defining a var requires a `;`, so the 2026-06-03 $CLAUDE_JOB_DIR observation conflated the two. Operative rule: LONE SIMPLE grep, SINGLE file arg, path resolves to the Edit target ($HOME resolves; arbitrary vars do not). SEPARATE FINDING (not a satisfier facet): native read-before-edit enforcement was ABSENT entirely in the dispatching main background child session (CLAUDE_CODE_CHILD_SESSION=1) -- a valid-match Edit on a pre-existing file that session never read and never created succeeded -- while BLOCKing in its subagents and in a `claude -p` child. Discriminator unidentified; enforcement is context-dependent at 2.1.251.","observed_single_file_grep":"ALLOW","observed_single_file_grep_quoted":"ALLOW","observed_compound_literal_path":"BLOCK","observed_unexpanded_HOME_simple":"ALLOW","observed_unexpanded_TMPDIR_simple":"BLOCK","observed_multi_file_grep":"BLOCK","observed_grep_tool":"BLOCK","observed_no_view_control":"BLOCK","observed_main_session_enforcement":"ABSENT"}
